# Commits that are pure reformatting (no logic change) — `git blame --ignore-revs-file
# .git-blame-ignore-revs` (or `git config blame.ignoreRevsFile .git-blame-ignore-revs`)
# skips these so `git blame` still points at the commit that actually changed a line.
#
# Add the SHA of the initial "chore: format with biome" commit here once it lands.
# It must be committed on its own, with no other changes, or this file can't cleanly
# separate "formatting" from "everything else" in blame output.
